## To debug c++
```sh
$ lldb main
```
Possible commands are `breakpoint set --name <function_name>`, `run`, `next`, `continue`, `p`

## To debug python
```sh
$ python3 -m pdb main.py
```
Check out, what you can do here?
